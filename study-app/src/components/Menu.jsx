import './Menu.css';

import React from "react";
// import { useAuth } from '../context/AuthContext';

function Menu() {
    // const { user } = useAuth();

    return (
        <div className="menu">
            <h2>Menu</h2>
        </div>
    );
}

export default Menu;